<?xml version="1.0" encoding="utf-8"?>
<!--
  Nothing leaves the device, including via backup.

  A cloud backup of the personal dictionary would move user text off device through a channel
  the app never opened itself, which would make the "fully offline" claim false in exactly the
  way nobody checks. Both cloud-backup and device-transfer are excluded wholesale.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </device-transfer>
</data-extraction-rules>
