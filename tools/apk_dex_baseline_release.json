{
  "generated_from": "app/build/outputs/apk/release/app-release-unsigned.apk",
  "why": "A clean build legitimately references network classes transitively via androidx/Compose/Kotlin stdlib. The gate reports the delta from this set, so a genuinely new network path is a finding while the unavoidable background is not. Re-baseline deliberately, after establishing where a new descriptor came from -- never to silence a finding.",
  "descriptors": [
    "Landroid/net/Uri$Builder;",
    "Landroid/net/Uri;"
  ]
}
